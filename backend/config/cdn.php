<?php

return [

    /*
    |--------------------------------------------------------------------------
    | CDN のベースURL
    |--------------------------------------------------------------------------
    |
    | ページ画像を配信しているホスト。API が返す画像URLの組み立てに使う。
    | ブラウザから直接叩かれるURLなので、コンテナ名ではなく
    | ブラウザから見えるホスト名を入れる (compose の環境変数で渡している)。
    |
    */

    'base_url' => rtrim(env('CDN_BASE_URL', 'http://localhost:8082'), '/'),

];
